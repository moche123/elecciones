[build]
  command = "npm run build && rm -rf site && mkdir -p site/elecciones && cp -r dist/* site/elecciones/"
  publish = "site"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/"
  to = "/elecciones/"
  status = 302
